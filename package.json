{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.13.1"
  },
  "scripts": {
    "start": "lerna run --parallel start:lerna",
    "test": "lerna run --parallel test"
  }
}
